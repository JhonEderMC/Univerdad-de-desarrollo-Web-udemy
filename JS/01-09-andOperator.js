// Range o value
const a = 7;
const minValue = 0, maxValue = 10;

if ( a >= minValue && a <= maxValue) {
     console.log(`${a} is between ${minValue} and ${maxValue} `);
} else {
    console.log(`${a} is not between ${minValue} and ${maxValue}`);
}